# this is a document comment

project = "Example Project"

[tool.ruff.lint.people]
name = "Nicholas Wlezien"

[tool.ruff.lint] # lint table comment
pydocstyle = { convention = "numpy" }

# this is the first comment for lint table

[tool.ruff]
line-length = 88
# this is a tool.ruff comment

[tool.ruff.lint.people.roles.managers]
# the manager for each person
names = [ "Elon Musk" ]

[tool.ruff.lint.people.roles]
# all roles
role = [ "Software Engineer" ]

[tool.rye]
managed = true
dev-dependencies = [
    "ruff>=0.4.4", # ruff version
    "mypy>=0.812",
    "sphinx>=3.5", # sphinx version
    "setuptools>=56.0" # setuptools version
]